import React, { Component } from 'react'

export default class RefreshButton extends Component {
    render() {
        return (
            <button type="button">
                Refresh
            </button>
        )
    }
}
